package test;

import org.junit.jupiter.api.Test;
import production.PlayerRedPanel;
import javax.swing.*;
import static org.junit.jupiter.api.Assertions.*;

class PlayerRedPanelTest {

   /* @Test
    void testGetCurrentRedPlayerWhenHumanSelected() {
        PlayerRedPanel panel = new PlayerRedPanel();
        JRadioButton redHumanButton = panel.getRedHuman();
        redHumanButton.setSelected(true);
        assertEquals("Human", panel.getCurrentRedPlayer());
    }*/


  /*  @Test
    void testGetCurrentRedPlayerWhenComputerSelected() {
        PlayerRedPanel panel = new PlayerRedPanel();
        JRadioButton redComputerButton = panel.getRedComputer();
        redComputerButton.setSelected(true);
        assertEquals("Computer", panel.getCurrentRedPlayer());
    }*/
}